DualDisplay:

    TopScreen:
    	background: 'apps/historicsites/bg-top.png'

    BottomScreen:
    	background: 'apps/historicsites/bg-bottom.png'
        SHSMap:
            id: map
        Label:
            text: "ZOOM: {0:g}".format(map.scatter.scale)
            font_size: 200

        BackToMenuButton:
